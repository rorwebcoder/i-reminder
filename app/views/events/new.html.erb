
<% content_for :page_title do %>
		Create Event
<% end %>

		<%= simple_form_for @event, :html => { :class => 'form-horizontal' } do |f| %>
				<%= render :partial => "form", :object => f, :locals => { :event => @event } %>
				<div class="form-actions">
						<%= f.button :submit, :value => "Create Event" %>
				</div>
		<% end %>
  